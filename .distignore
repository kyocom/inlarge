.git
.github
.gitignore
.distignore
.DS_Store
README.md
# Directory assets (icon, screenshots): published to SVN assets/, never shipped
# inside the plugin download.
.wordpress-org
# Artwork exports sometimes land next to the shipped JS by mistake. The plugin
# package needs neither, so keep them out even if a copy reappears here.
assets/icon.svg
assets/icon-*.png
assets/screenshot-*.png
*.zip
node_modules
.editorconfig
